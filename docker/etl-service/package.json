{
  "name": "oncologia-etl",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "load-oltp": "bun run scripts/load-oltp.ts",
    "load-sinadef": "bun run scripts/load-sinadef.ts",
    "load-ci10": "bun run scripts/load-ci10.ts",
    "load-all": "bun run load-ci10 && bun run load-oltp && bun run load-sinadef",
    "quality": "bun run scripts/quality-check.ts",
    "all": "bun run load-all && bun run quality"
  },
  "dependencies": {
    "mongodb": "^7.2.0",
    "neo4j-driver": "^6.0.1",
    "papaparse": "^5.5.3",
    "postgres": "^3.4.5",
    "xlsx": "^0.18.5"
  }
}
